import React from 'react';
import useTitle from 'src/hooks/useTitle';

export default function BoardsPage() {
  useTitle('BoardsPage');
  return (
    <main>
      <h1>BoardsPage</h1>
    </main>
  );
}
