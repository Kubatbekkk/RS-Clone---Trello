import React from 'react';
import useTitle from 'src/hooks/useTitle';

function LoginPage() {
  useTitle('LoginPage');
  return (
    <main>
      <h1>
        Login Page
      </h1>
    </main>
  );
}

export default LoginPage;
